/**
 * Response DTOs — Outgoing data shapes.
 *
 * These classes define what the backend sends BACK to the frontend in response bodies.
 * They carefully select which fields to expose, never leaking sensitive data
 * like passwords or internal database IDs unnecessarily.
 */
package com.smarthire.dto.response;
