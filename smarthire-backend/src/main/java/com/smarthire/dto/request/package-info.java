/**
 * Request DTOs — Incoming data shapes.
 *
 * These classes define what the frontend sends TO the backend in request bodies.
 * Each has validation annotations (@NotBlank, @Email, @Size) to ensure
 * data integrity before it reaches the service layer.
 */
package com.smarthire.dto.request;
